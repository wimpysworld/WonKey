# Authentic identify/readback capture

These seven files are unchanged copies of the user's capture from 11 September 2026:

`/home/martin/.local/state/agent-reviews/key/worktree-key/run-20260911T150408Z-dMkT7Y/captures/20260911T162302.726391430Z-3220202532`

The capture confirms model `0112`, version `1014`, identifier `be077ba2`, key prefix `0001000128`, and RGB bytes `01ffffff`.
The original provenance contains all four USB report descriptors. The original files retain their capture-time warnings and paths.

This is query evidence, not a firmware backup or proof of configuration upload, restore, or persistence after reconnect.
All transport state-machine tests use synthetic devices. They never open a real device.
